import * as types from "../constants/actionTypes";

const initialState = {
    totalPostcards: 0,
}

const postcardReducer = (state = initialState, action) => {
    switch (action.type) {
        case types.ADD_POSTCARD:
            let postcardList;
            const newPostcard = {

            }
    }
}